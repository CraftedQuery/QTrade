{
  "$defs": {
    "DatasetSplit": {
      "description": "Which walk-forward split a prediction belongs to.",
      "enum": [
        "train",
        "validation",
        "holdout"
      ],
      "title": "DatasetSplit",
      "type": "string"
    }
  },
  "$id": "https://ai-trading-lab.local/schemas/prediction.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "One model output, stored before its outcome is known.\n\nThere is deliberately no realised-return or correct/incorrect field. An\noutcome is a separate record joined on ``prediction_id`` later, so that a\nstored prediction can never be quietly revised once the future arrives.",
  "properties": {
    "as_of": {
      "description": "Decision time the prediction was made for.",
      "format": "date-time",
      "title": "As Of",
      "type": "string"
    },
    "created_at": {
      "description": "Wall-clock time the prediction was written.",
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "experiment_id": {
      "description": "Experiment that produced it.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Experiment Id",
      "type": "string"
    },
    "feature_snapshot_id": {
      "description": "Snapshot the model consumed.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Feature Snapshot Id",
      "type": "string"
    },
    "horizon": {
      "description": "Forward span the prediction covers.",
      "format": "duration",
      "title": "Horizon",
      "type": "string"
    },
    "model_version": {
      "description": "Version of the model that produced it.",
      "title": "Model Version",
      "type": "string"
    },
    "prediction_id": {
      "description": "Unique prediction id.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Prediction Id",
      "type": "string"
    },
    "schema_version": {
      "default": "0.1.0",
      "description": "Contract version this record was written under.",
      "title": "Schema Version",
      "type": "string"
    },
    "split": {
      "$ref": "#/$defs/DatasetSplit",
      "description": "Walk-forward split this prediction belongs to."
    },
    "symbol": {
      "description": "Instrument the prediction is about.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Symbol",
      "type": "string"
    },
    "value": {
      "description": "Predicted score or expected return.",
      "title": "Value",
      "type": "number"
    }
  },
  "required": [
    "prediction_id",
    "experiment_id",
    "model_version",
    "symbol",
    "as_of",
    "horizon",
    "feature_snapshot_id",
    "value",
    "split",
    "created_at"
  ],
  "title": "Prediction",
  "type": "object",
  "x-contract-version": "0.1.0"
}
