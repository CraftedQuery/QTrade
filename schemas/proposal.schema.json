{
  "$defs": {
    "ProposalLine": {
      "additionalProperties": false,
      "description": "A single desired position within a proposal.",
      "properties": {
        "reference_price": {
          "description": "Price used to translate weight into quantity.",
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "title": "Reference Price",
          "type": "string"
        },
        "symbol": {
          "description": "Instrument to hold.",
          "maxLength": 128,
          "minLength": 1,
          "title": "Symbol",
          "type": "string"
        },
        "target_weight": {
          "description": "Desired share of portfolio value, in [0, 1].",
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "title": "Target Weight",
          "type": "string"
        }
      },
      "required": [
        "symbol",
        "target_weight",
        "reference_price"
      ],
      "title": "ProposalLine",
      "type": "object"
    }
  },
  "$id": "https://ai-trading-lab.local/schemas/proposal.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A desired target portfolio produced by a strategy.\n\nWeights are bounded below at zero by :data:`~lab.contracts.base.Weight`, so a\nshort position cannot be expressed. Any weight not allocated to a line is\ncash; see :attr:`cash_weight`.",
  "properties": {
    "as_of": {
      "description": "Decision time the proposal targets.",
      "format": "date-time",
      "title": "As Of",
      "type": "string"
    },
    "created_at": {
      "description": "Wall-clock time the proposal was written.",
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "experiment_id": {
      "description": "Experiment that produced it.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Experiment Id",
      "type": "string"
    },
    "lines": {
      "description": "Desired positions. May be empty (all cash).",
      "items": {
        "$ref": "#/$defs/ProposalLine"
      },
      "title": "Lines",
      "type": "array"
    },
    "proposal_id": {
      "description": "Unique proposal id.",
      "maxLength": 128,
      "minLength": 1,
      "title": "Proposal Id",
      "type": "string"
    },
    "schema_version": {
      "default": "0.1.0",
      "description": "Contract version this record was written under.",
      "title": "Schema Version",
      "type": "string"
    },
    "strategy_version": {
      "description": "Version of the strategy code.",
      "title": "Strategy Version",
      "type": "string"
    }
  },
  "required": [
    "proposal_id",
    "experiment_id",
    "strategy_version",
    "as_of",
    "lines",
    "created_at"
  ],
  "title": "Proposal",
  "type": "object",
  "x-contract-version": "0.1.0"
}
